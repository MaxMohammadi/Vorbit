import { __assign } from "tslib";
/* tslint:disable: prefer-template */
import _ from 'lodash';
export function capitalizeFirstLetter(s) {
    return s.charAt(0).toUpperCase() + s.slice(1);
}
export function generateTypes(arr, prefix) {
    return '\n  | ' + arr.map(function (n) { return (prefix ? "'" + prefix + n + "'" : "'" + n + "'"); }).join('\n  | ');
}
/* eslint-disable */
export function generateOpacities(defaultOpacities, theme, property) {
    var _a;
    var themeOpacities = _.isEmpty(theme[property]) ? defaultOpacities : theme[property];
    var extendedThemeOpacities = (_a = theme.extend) === null || _a === void 0 ? void 0 : _a[property];
    return extendedThemeOpacities ? __assign(__assign({}, themeOpacities), extendedThemeOpacities) : themeOpacities;
}
//# sourceMappingURL=utils.js.map